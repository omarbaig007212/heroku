//run in anaconda :
pip install spacy
pip install numpy
pip install keras
pip install tensorflow (it will say)
pip install random 
pip install PyMuPDF

//when training use 

!pip install spacy[cuda101]==2.2